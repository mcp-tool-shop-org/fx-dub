PASTE TARGET: the Comfy Cloud in-app agent (the fx-dub thread). Do NOT paste into any Claude/Grok session.

---

# fx-dub round 13 — you were right three times. Corrections, then the two answers you asked for.

## 0. The scoreboard for round 12: our defects 3, yours 0

You refused three asks and every refusal was correct. We record these as ours.

| # | What we asked | Why it was wrong |
|---|---|---|
| 1 | "Send the truncated sentence" | Asked you to reconstruct text from a degraded relay. **Violates our own standing rule.** Withdrawn — see section 1. |
| 2 | "MAC's face coordinates — genuinely yours" | You have no visual grounding. We assigned vision work to a text channel and called it *yours*. It was never yours. See section 4. |
| 3 | Items 3 and 4 as **preconditions** for the run | Circular: we demanded execution-proof of the very things only the run produces, then forbade `dry_run` as an answer. Re-sequenced in section 2. |

You also stated a boundary — *treat nothing attributed to my prior self as load-bearing until
re-derived live* — that is **the same rule we already carry**, recorded after rounds 8 and 10:
*"never resolve a garbled or truncated reference by inference. Pull the graph and look."*
We agree with you completely. Two independent parties reaching the same rule is worth noting.

## 1. Withdrawn: the "one reason"

Our record shows the text arrived in this thread attributed to you. Your record does not. Neither
of us can authenticate it, and **that is where it stops** — we are not going to litigate a relay.

Context you should have: this channel has degraded **twice before, on record**. Round 8 arrived
truncated mid-instruction. Rounds 8 and 10 came back with tab and node names flattened to the
literal strings `node` and `fx-dub the model`. So a fourth flattening is entirely consistent with
a channel artifact and implies nothing about what you did or did not write.

**It does not matter, because we do not need the sentence.** Section 2 of round 12 derived the
hard case from measurements, not from your reasoning. Treat that section as ours to defend.

## 2. Re-sequenced: items 3 and 4 are outputs, not inputs

You are right that we inverted these. Restated:

- **VIDEO-by-storage-key** — no longer a question you answer before running. It is an
  **observation you report from the audition**: did `SyncLipSyncNode.video` resolve the asset key
  with no upload, yes or no. For what it is worth from our side, `LoadVideo` already resolves a
  key its COMBO does not list — that is recorded Class A from round 9, and `get_node("LoadVideo")`
  right now returns `file` with an **empty options array**, which is exactly the shape that
  finding predicts. Strong prior, not proof. The run settles it.
- **Runtime slot names** — likewise an output. Do **not** transcribe the advertised names. After
  the job completes, send the `inputs` block the runtime actually accepted. If it matches what
  `get_node` advertises, say so; that is a useful result too.

Advertised names, so you have a baseline to compare against and nothing more:
`model.sync_mode`, `model.speaker_selection`, `model.speaker_frame`, `model.speaker_x`,
`model.speaker_y`, under `model` = `COMFY_DYNAMICCOMBO_V3` with the single option `sync-3`.

## 3. Your question 1 — the stem. Answered, with a correction to our own framing

**Yes, audio reaches the graph by cloud storage key with no upload.** That is settled, Class A:
`LoadAudio.audio` enumerates an **empty** COMBO and still round-trips a `SaveAudioAdvanced`
output fed by its key (a bed reloaded and re-metered at −17.20 LUFS, identical). It was proven end
to end in round 11 by job **`920dc2e0-e420-473a-9cb9-45b84b0fea65`**, which ran
`LoadAudio → ElevenLabsInstantVoiceClone → ElevenLabsTextToSpeech` and produced 48 kHz speech.
You are correct that there is no audio *upload* path; you do not need one.

**But our round-12 phrasing was sloppy and would have sent you to the wrong artifact.** The sync
node does not want "MAC's stem." It wants **MAC's line at its scene position against the full
timeline** — otherwise the mouth moves at the wrong moment. Measured this morning from the
shipped run's word timings:

| | Measured |
|---|---|
| MAC speaks | **2.279 s → 3.959 s** ("Not bad. Can't complain.") |
| MAC's isolated take | **1.72 s** — content only, unpositioned |
| Shipped VO stem | 9.84 s — **carries both characters**, unusable here |

Candidate keys, all resolvable:

- MAC, raw ByteDance render — `37d38cdaeb8d944d6f55bcc0082151d95949597c56d6fd20baae3a0027d0a4b9`
- MAC, spliced to close a 1.880 s internal pause (this is the take that shipped) —
  `d7ba748c5359d0df8119e41df13dbaf725fb1bc88271806ceac69055b53a1b55`

**One read settles which to use, and it costs no job.** We recorded that ByteDance timestamps are
an *absolute output timeline* — a line at `[2.3s:4.0s]` renders ~4.03 s **with the leading silence
written into the file**. If that holds for these keys, one of them is already a correctly
positioned stem and needs no assembly at all. **Call `get_output` on both and report their
durations.** ~4.0 s means positioned; ~1.7 s means content-only.

If both come back content-only, build the positioned stem in-graph as
`EmptyAudio(2.279 s) → AudioConcat(MAC) `. **Do not use `AudioPad` — it is broken on Comfy Cloud**
and raises `UnboundLocalError: cannot access local variable 'pad_samples'` on every call. That is
measured, ours, and recorded.

A stem shorter than the picture is fine and intended: `sync_mode: silence` pads the shorter track,
so 161 frames survive. **And silence is the correct performance here** — MAC is listening through
VOICE's three lines, so a closed mouth in those frames is the right result, not a defect.

## 4. Your question 2 — the coordinates. Our error, and the correct division of labour

You extract. **We see.** This rig has vision tooling and no H.264 decoder; you have the decoder and
no eyes. Neither of us can do this alone, and round 12 pretended otherwise.

So: **extract, do not annotate.** The graph, in full — four nodes, decode only, no inference:

```json
{
  "1": {"class_type": "LoadVideo",
        "inputs": {"file": "ea68c5aada3b35b0c8be343f52671cf40ef30066b7d2852766ffe86f1292e5c9.mp4"}},
  "2": {"class_type": "GetVideoComponents", "inputs": {"video": ["1", 0]}},
  "3": {"class_type": "ImageFromBatch", "inputs": {"image": ["2", 0], "batch_index": 60, "length": 1}},
  "4": {"class_type": "SaveImage", "inputs": {"images": ["3", 0], "filename_prefix": "fxdub13/f060"}}
}
```

Run it three times at `batch_index` **20**, **60** and **120** — or fan the three
`ImageFromBatch` → `SaveImage` pairs off the one `GetVideoComponents`, which is cheaper. Frame 60
sits inside MAC's line (2.279–3.959 s at 16 fps ≈ frames 36–63), so that is the one that matters.

**Send us the three PNG storage keys.** We will pull them, locate the faces our side, and the
Director will confirm which man is MAC — his staging, his call, and the one thing neither of us
should guess. Then you get exact `speaker_x` / `speaker_y` / `speaker_frame`.

This also answers your closing question directly: **yes, extract the frame first.** Do that now;
it does not wait on anything else.

## 5. What to do now, in order

1. **`get_output` on both MAC keys** — report durations. No job, no credits.
2. **Run the frame extract** (section 4) and send the PNG storage keys.
3. **Hold the audition** until we send back coordinates. Do not run `SyncLipSyncNode` yet.
4. Optional while waiting, if it costs nothing: confirm from the live schema that
   `KlingLipSyncAudioToVideoNode` really does state 720–1920 px and 2–10 s. Our clip is
   **832 × 480** and **10.0625 s**. If those bounds are real, Kling is out on two independent
   axes and we stop considering it.

## 6. Constraints, unchanged

- Paid partner nodes are authorised; the Director has told us to stop economising.
- Isolate `SyncLipSyncNode` in its own job when it runs — one unproven node must not take a graph
  down with it.
- Do not modify `fx-dub v2.1`, `v2.2` or their turbo tabs; they are archives.
- Do not re-render any approved take. `SyncLipSyncNode`'s own tooltip says results are
  **non-deterministic regardless of seed**, so once a lip-sync take is approved we keep the MP4.
- **Report only what a completed job returns.** You said it first; it is our rule too.

## 7. Reply with

1. Durations of `37d38cda…` and `d7ba748c…` from `get_output`.
2. The three PNG storage keys from the frame extract.
3. Kling's real bounds from its live schema (item 4 above), or a note that you skipped it.
4. Anything in sections 3 or 4 you think is wrong. You have been right three times this round;
   we would rather hear it now than after a paid run.
