PASTE TARGET: the Comfy Cloud in-app agent (the fx-dub thread). Do NOT paste into any Claude/Grok session.

---

# fx-dub round 11 — find us a real character voice. Survey the whole TTS surface, measure it, report.

## Where we are

`fx-dub` now runs end to end and passes its full contract: **19/19** on our receipt checker —
48 kHz mix at −18.18 LUFS, dialogue 10.86 LU above the ambience bed, re-muxed MP4 carrying
audio with all 161 frames intact. The pipeline is not the problem.

**The voices are the problem.** The Director's verdict on everything we have tried: *"None of
these sound very natural, and only Brian is anywhere near deep enough."*

The scene is a 10.06 s night-street shot. Two men talk; one is off-frame:

```
VOICE (off-frame, must be DEEP and gritty):  Hey, how's it going?
MAC   (on-frame, gritty, weary):             Not bad. Can't complain.
VOICE:                                       Good to hear, good to hear.
VOICE:                                       Hey, tell Charlie I got that thing for him,
                                             whenever he wants to drop by.
```

## What we have already measured — do not re-run these

| Engine | Node | Result | Measured |
|---|---|---|---|
| Chatterbox | `FL_ChatterboxTTS` | default voice, wrong character | 24 kHz mono, −16.22 LUFS |
| Qwen3-TTS VoiceDesign | `FB_Qwen3TTSVoiceDesign` | takes a written character brief in `instruct`; **rejected as unnatural** | 24 kHz mono |
| Qwen3-TTS presets | `FB_Qwen3TTSCustomVoice` | 9 speakers (Aiden/Dylan/Eric/Ono_anna/Ryan/Serena/Sohee/Uncle_fu/Vivian) | 24 kHz mono |
| Qwen3-TTS clone + dialogue | `VoiceClonePrompt` → `RoleBank` → `DialogueInference` | **works**, multi-role with per-punctuation pauses; voices still unnatural | 24 kHz mono |
| ElevenLabs presets | `ElevenLabsVoiceSelector` + `ElevenLabsTextToSpeech` (`eleven_v3`) | best so far; **Brian** is the only one near deep enough, still not natural enough | **48 kHz**, `opus_48000_192` |

Two platform facts we established and you can rely on:

1. **`LoadAudio` resolves a cloud storage key that its COMBO does not list** — same as
   `LoadVideo`. Any `SaveAudioAdvanced` output can be reloaded by its content hash. We proved
   an exact round-trip (a bed reloaded and re-metered at −17.20 LUFS, identical). **This means
   reference audio CAN reach a voice-cloning node.** The old "cloud has no audio-upload path,
   so cloning is local-only" rule is dead.
2. Every *local* TTS on cloud decodes 24 kHz mono; **ElevenLabs emits 48 kHz**, which removes
   the rate mismatch at source instead of relying on `AudioMix` adopting `audio_1`'s rate.

## What we want from you

**Survey the entire text-to-speech surface available on Comfy Cloud and report it measured, not
described.** We keep picking from the first list we find; we want the whole map before choosing
again. Specifically:

1. **Enumerate every TTS / voice node on the platform** — core, partner, and custom packs.
   We know of ElevenLabs, Qwen3-TTS, FL_Chatterbox, Fish Audio, ByteDance Seed Audio, MiniMax.
   **Tell us what else exists that we have not named.** For each: node class, whether it is a
   paid API node, output sample rate and channel count, and what voice-selection mechanism it
   offers (preset list / text description / reference-audio clone / voice ID).

2. **For every engine that exposes a preset voice list, give us the FULL list** with whatever
   descriptors the platform carries (age, timbre, accent, character). We only ever saw
   ElevenLabs' 21 and Qwen3's 9. If Fish Audio, ByteDance or MiniMax carry deeper/rougher
   character voices, we want their names.

3. **Report every knob that shapes delivery**, not just voice identity — speed, stability,
   style/exaggeration, emotion tags, similarity boost, temperature. ElevenLabs `eleven_v3`
   reportedly supports inline audio tags; **if that is real on this platform, show us the exact
   syntax that works**, tested, not quoted from documentation.

4. **The cloning question is the important one.** `ElevenLabsInstantVoiceClone` takes 1–8
   recordings and `FishAudioInstantVoiceClone` takes 1–20. Given that `LoadAudio` can now
   deliver reference audio by storage key: **confirm whether an AUDIO link from `LoadAudio`
   actually feeds those clone nodes**, and what their `files` / `COMFY_AUTOGROW_V3` inputs
   expect in API format. If cloning works, that is our answer — the Director supplies a
   reference and we clone the real thing instead of shopping a preset list.

5. **Then audition, don't theorise.** Generate this exact line —
   *"Hey, tell Charlie I got that thing for him, whenever he wants to drop by."* — through the
   most promising **six** deep/gritty/older-male options you have found, across at least
   **three different engines**. Save each as its own FLAC under `fxvox5/<engine>_<voice>`.
   Report the node settings for each so we can reproduce it exactly.

## Constraints

- **Paid partner-API nodes are authorised.** The Director has credits to burn and has told us
  explicitly to stop economising. Use the good models. Do not pick the free option to save money.
- Prefer **48 kHz** output where the engine offers it.
- The full four-line exchange must fit inside **10.06 s**, so prefer engines with a native
  `speed` control. Do **not** post-process with a time-stretch; we tried that and it was the
  wrong answer.
- Do not modify `fx-dub v2.1` or `fx-dub v2.2` — they are archives.

## Reply with

1. The full TTS/voice node inventory (item 1), with the measured fields.
2. The complete preset voice lists (item 2).
3. The delivery-knob map, including whether `eleven_v3` audio tags actually work (item 3).
4. A definitive yes/no on `LoadAudio` → clone-node wiring, with the API-format shape (item 4).
5. Six audition FLACs + their exact settings (item 5), and your own ranking of which two are
   the deepest and most natural.

**No dub runs.** We only want voice candidates this round; we assemble the scene our side.
