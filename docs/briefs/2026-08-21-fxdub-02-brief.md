PASTE TARGET: the Comfy Cloud in-app agent (the tab where you built "Describe a video (Florence2)" and the two demos). Do NOT paste into any Claude/Grok session.

---

# fx-dub round 2 — verification results + v2 build order (VALIDATE ONLY, no runs)

Good build, and thank you for the honest architecture notes (caption ≠ wire, caption ≠ script — both confirmed by our research). We pulled all three of your tabs over the API and verified the demos from our side. Everything below is measured, not vibes.

## What we verified (our side)

- **Billing feed:** exactly two jobs in your demo window — SFX `d3cda2f7-bc64-413e-ae14-dc0d279e5dff` = 7.63 gpu-sec, dialogue `19c22524-a7b0-48ee-bb99-37e1651e8067` = 6.54 gpu-sec, both rtx_pro_6000. Nice and cheap; your dialogue run is our first end-to-end FL_ChatterboxTTS cloud measurement — noted with credit.
- **Output headers (downloaded + decoded):** your ACE demo decodes **44,100 Hz stereo 16-bit, 9.938 s**; your ChatterBox demo decodes **24,000 Hz MONO 16-bit, 5.800 s**.

## Corrections to carry (you'll want these in your own answers too)

1. You described Stable Audio Open as "permissive." It is **not** — it's the **Stability AI Community License** (free commercial only under $1M/yr revenue) **plus a Gemma-ToU rider on the T5Gemma text encoder**. We verified this against the license texts. Please label it "conditional," never "permissive."
2. You built on **ACE-Step 1.0** (`ace_step_v1_3.5b.safetensors`, Apache-2.0). **ACE-Step 1.5 is MIT on code AND weights** and is the better model — the v2 build below ports to it. The 1.5 files are already exercised on this account (`acestep_v1.5_xl_base_bf16.safetensors`, `ace_1.5_vae.safetensors`, `qwen_0.6b_ace15` + `qwen_4b_ace15`).
3. Your main tab mixes **44.1 kHz stereo with 24 kHz mono** through `AudioCombine(mean)` with no resample, no upmix, no level control. That's the biggest defect in v1 — v2 fixes it below.
4. `SaveAudio` is deprecated (your own node title says so). Masters go through **`SaveAudioAdvanced`**, FLAC.
5. Florence with `do_sample: true` gives non-reproducible captions (and forfeits memo-cache pricing on re-runs). Pin `do_sample=false`, fixed seed, and use `microsoft/Florence-2-large` (matches our measured captioner).
6. `frame_load_cap: 0` + `select_every_nth: 1` sends EVERY frame to Florence — a 60 s clip is ~1,700 captioned frames of GPU time. Sample sparsely.

## Build order — new tab **"fx-dub v2"** (leave v1 tabs untouched; version-bump on structural change)

**Ingest**
- `VHS_LoadVideo`: `frame_load_cap = 8`, `select_every_nth` sized so the 8 frames spread across the whole clip; keep `skip_first_frames` small (0–4).
- Wire `video_info` → whatever node unpacks VHS_VIDEOINFO → **duration (FLOAT)** → the bed's `seconds`. (Q1 below.)

**Describe**
- `DownloadAndLoadFlorence2Model`: `microsoft/Florence-2-large`, fp16, sdpa.
- `Florence2Run`: `more_detailed_caption`, `do_sample=false`, `num_beams=3`, `seed=1` fixed, `max_new_tokens=1024`.
- **`caption` STRING → SaveText** (filename_prefix `fxdub/caption`) — the caption must reach the output manifest; on headless runs a dangling STRING is lost.

**SFX/ambience bed — port to ACE-Step 1.5**
- Loaders for `acestep_v1.5_xl_base_bf16` + `ace_1.5_vae` + the dual Qwen text encoders; `TextEncodeAceStepAudio1.5`.
- Sampler: euler / simple / 50 steps / cfg 6 / `ModelSamplingAuraFlow` shift 3. Seed = one shared `PrimitiveInt`, fixed.
- `tags` = a `PrimitiveStringMultiline` **prompt box seeded from the caption but editable** — do NOT hard-wire the raw caption into tags (our measured research: visual-only detail degrades audio; a rewrite step sits between them host-side).
- `duration` = the video duration float from ingest (cap 60 s for now).
- Decode → this branch is 48 kHz.

**Dialogue**
- `FL_ChatterboxTTS`, default voice (no audio_prompt — cloud-safe), text = must-edit placeholder, `seed` fixed.

**Mix bus (the heart of v2)**
- Resample BOTH branches to **48 kHz** and upmix the mono dialogue to stereo before combining. (Q2 below.)
- Per-branch gain staging: dialogue is the anchor; bed default **−15 LU under dialogue** (ambience) / −10 LU (music). Expose both gains as primitives.
- Combine (see Q3 on AudioCombine semantics — we need sum-without-normalize plus knowledge of clip behavior).
- `AudioLoudnessMeter` on the mix → **SaveText** manifest (house pattern: `Integrated Loudness: X LUFS`). Meter the dialogue stem too if a second meter is cheap.
- Masters: **`SaveAudioAdvanced` FLAC** — save the MIX and BOTH stems (bed, dialogue) as separate files (`fxdub/mix`, `fxdub/stem_bed`, `fxdub/stem_vo`).

**Mux-back (the actual dub deliverable)**
- `VHS_VideoCombine`: original frames + the mixed AUDIO → h264 mp4, fps propagated from ingest (Q4). Output `fxdub/dubbed`.

**Ground rules**
- Static validation green is the bar (headless consumer is `/api/prompt`); no UI-only nodes; runtime knobs as `PrimitiveFloat`/`PrimitiveInt`/`PrimitiveStringMultiline`.
- **VALIDATE ONLY. Do not run anything** — the Director auditions configurations before any generation spend. Report validation state and the exact node class names you used.
- Label anything you couldn't verify in the live catalog as inference, per your Class A/B practice (which we appreciate).

## Questions (your live-catalog sight beats ours)

1. **VHS_VIDEOINFO → FLOAT:** which node on the allowlist unpacks `video_info` into duration/fps floats? (`VHS_VideoInfo` / `VHS_VideoInfoLoaded`?)
2. **Resample + upmix:** which allowlisted nodes do sample-rate conversion (target 48 kHz) and mono→stereo? Core or a pack?
3. **AudioCombine semantics:** does `method=add` sum-with-clipping? Does `mean` halve both? Is there a gain/volume node (dB or linear) on the allowlist for the −15 LU offset, and does anything resample implicitly when rates mismatch?
4. **VHS_VideoCombine:** confirm it accepts an AUDIO input on cloud, which formats/containers are available headlessly, and that fps can be driven from the loaded video's info.
5. **Shot/onset detection:** is there ANY node on the allowlist that does shot-boundary detection or audio onset detection? (Roadmap: per-shot spot effects with timed placement.)
6. **Qwen3-TTS:** confirm the exact node class names for preset/designed-voice TTS (`FB_Qwen3TTS…`?) as a second VO option, and whether output sample rate is documented.
7. **Audio gain/pan/delay utilities:** list what exists on the allowlist for timeline placement (delay/offset a clip by N seconds, concat with crossfade) — needed for the spot-effects layer next round.

Reply with: validation state of "fx-dub v2", the node-class list, answers Q1–Q7, and anything you had to substitute (with the reason). We'll pull the tab over the API and verify before any run order.
