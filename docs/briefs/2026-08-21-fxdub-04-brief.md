PASTE TARGET: the Comfy Cloud in-app agent (the fx-dub thread). Do NOT paste into any Claude/Grok session.

---

# fx-dub round 4 — recovery handled our side; GO on v2.1 in a new tab (VALIDATE ONLY)

Good stop-and-ask — that was the right move. Here's the state, verified from our side, and your answers.

## Q1 — the clobber was worse than you thought, and it's already fixed

Your "fx-dub v2 is a saved draft, not lost" assumption was wrong: **the server-side file `fx-dub v2.json` was clobbered too** — we pulled it and it holds the 19-node Motif stack. `open_workflow` replaced the canvas AND the saved file.

But we hold the bytes (we archive every pull), so we already restored it: **the true 29-node v2 now lives at workflow record `65a063a5-9342-4297-8cfa-01313178fab9`, name "fx-dub v2"** — re-pulled and verified after the restore. Therefore:

- **Do NOT touch record `65a063a5…`** — it's the archival copy; you don't need it for the build.
- **Leave the clobbered file `fx-dub v2.json` exactly as-is.** It's now just a duplicate of the Motif record; the Director will delete it or not. Do not "restore" it, do not build on it, do not save over it.
- **Standing rule, codified (your own proposal, adopted): `open_workflow` only ever into a NEW tab.** Never while focused on a tab you care about.

## Q2 — GO. Build "fx-dub v2.1" in a brand-new tab.

Your mirrored recipe is confirmed from our side — we independently verified every class you named is live in the catalog: `UNETLoader` (`acestep_v1.5_xl_base_bf16.safetensors`) + `VAELoader` (`ace_1.5_vae.safetensors`) + `DualCLIPLoader` (`qwen_0.6b_ace15` + `qwen_4b_ace15`, type `ace`) + `ConditioningZeroOut` negative + **`EmptyAceStep1.5LatentAudio`** (good catch — v2 was using the 1.0 latent node with the 1.5 stack) + `ModelSamplingAuraFlow` shift 3 + euler/simple/50 steps/cfg 6 + `VHS_SelectEveryNthImage`.

Carry the v2 mix bus over unchanged (it was right): AudioStandardize(stereo) ×2 → AudioMix with primitive gains (VO 0 dB / bed −15 dB) → meters ×2 → SaveText ×2 → SaveAudioAdvanced FLAC ×3 (mix, stem_bed, stem_vo) → VHS_VideoCombine h264-mp4 with frame_rate ← `source_fps`, plus the Florence block (large / fp16 / sdpa / `do_sample=false` / seed 1) and caption → SaveText.

The full fix list, restated so nothing drops:

- **F1** ✔ in your recipe — negative via `ConditioningZeroOut` (never the positive output into both KSampler slots).
- **F2** ✔ in your recipe — the XL-base split stack above. *(Optional second tab "fx-dub v2.1-turbo" with your AIO checkpoint + the official ACE-1.5 turbo template's sampler block, read from the template — only if cheap for you.)*
- **F3** — on the loader: `skip_first_frames=0`, `frame_load_cap=0` (the mux needs the full stream). Insert `VHS_SelectEveryNthImage` between loader IMAGE and `Florence2Run` ONLY, `select_every_nth` as a primitive/widget defaulted to ~30 (≈1 frame/sec at 30 fps; it can't auto-derive without a math node, so it's a documented knob).
- **F4** — `timesignature: "4"`.
- **F5** — answer the open question: is there ANY float min/clamp/math node on the allowlist? If yes: `VHS_VideoInfo.source_duration` → clamp(≤60) → encoder `duration` + latent `seconds`, drop the manual knob. If no: keep the manual duration knob and state "no float math on allowlist" explicitly in your reply.
- **F6** — keep: `trim_to_audio=false`, h264-mp4 / yuv420p / crf 19, save_metadata true.

## Reply with

1. Validation state of "fx-dub v2.1" (+ "-turbo" if built): errors/warnings, ready_to_run.
2. Disposition per F1–F6 (done / substituted-with-reason / blocked), including the F5 float-math answer.
3. Exact node-class list.
4. Anything from the reference record you could not mirror, with the schema reason.

**No runs.** The Director auditions the configuration first; the audition run also closes the mix-bus output-rate question from our side (we decode the FLAC header).
