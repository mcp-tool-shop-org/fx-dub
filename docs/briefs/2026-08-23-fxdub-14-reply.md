# fx-dub round 14 - REPLY (the agent's words, as relayed by the Director 2026-08-23)

> Transcribed from the Director's paste. Flattening artifact present again - node class names and
> one tool name rendered as empty `` placeholders. Paired verification:
> [`2026-08-23-fxdub-14-verification.md`](2026-08-23-fxdub-14-verification.md).

---

This is the discrepancy you asked me to catch - and you were explicit that it's worth more than the audition, so I'm stopping on it rather than proceeding.

**The discrepancy is real, and it's the opposite direction from a "lean view"**

My live ("node"), pulled just now, returns four inputs:

- video (VIDEO, required)
- audio (AUDIO, required)
- seed (INT, required)
- model (COMFY_DYNAMICCOMBO_V3, required, choices ["sync-3"])

That's it. No required-type array of nine. No input_details. No sync_mode, speaker_selection, speaker_frame, speaker_x, speaker_y - not as flat fields, not as conditionals, not marked applies_when: ["sync-3"]. My tool surface does not carry an input_details structure at all, so the "conditional sub-fields nested behind the " that your get_node returned this morning are not visible on my surface in any form.

So this is not "a lean view collapsed them and the full view has them." My fullest view is these four. Your surface shows nine with dotted conditionals; mine shows four with none. That is a genuine divergence between our two tool surfaces, and per your section 4 it is the finding to chase before spending anything.

**What it means for the plan, concretely**

I cannot set model.speaker_selection = "coordinates", model.speaker_x, or model.speaker_y on my side. There is no widget address for them to exist at. If I build this node and hand it your pending coordinates, `` will fail unknown_widget - the fields aren't on the node I can see. The COMFY_DYNAMICCOMBO_V3 hint you quoted ("send each conditional sub-field using its full dotted name") describes a mechanism my surface doesn't expose, which means even the dotted-name workaround has nothing to attach to here.

Which also means: your entire coordinate-targeting plan - the frame extract, the vision annotation, speaker_frame=60, speaker_x/y - depends on inputs that exist on your tool surface and not on mine. If I build blind against my four-input view, I produce a node that runs with default speaker behavior and silently ignores every coordinate you send. That's exactly the "advertised is not runtime" trap, except one layer earlier: advertised differs between surfaces before runtime even enters.

**What I did not do**

I did not create the fx-dub v3-sync tab, did not extract any frames, did not touch any canvas. Nothing was built. Your untouchables (v2.1, v2.2, both turbo tabs, and the current focus) are all as they were - I ran one read-only schema call and nothing else.

**The one question that decides everything**

Two possibilities, and they fork the whole build:

1. Your surface is right and mine is truncated at the source - the conditional sub-fields are real, my `` is dropping the input_details block, and coordinate targeting is buildable from your side or a fuller tool but not from mine.
2. The nine-input schema on your side is itself a relay artifact - the same channel that flattened node names to node twice could plausibly have inflated a four-input array into nine with plausible-sounding field names. I can't rule this out, and neither of us should want to spend a paid run to find out which.

I'm not going to guess which. Given your standing rule - pull the graph and look, never resolve a divergence by inference - the next move is a second independent read on your side of just the required-type array count for this exact node, this session: does get_node("node").inputs.required return 4 entries or 9 right now? If it still returns 9 while mine returns 4, we have a confirmed live divergence and the audition must wait until we know which surface the paid run will actually honor. If yours has also settled to 4, then this morning's nine-input read was the artifact and the coordinate plan is void - back to auto-detect.

Tell me that count and I'll act on it immediately. I won't build a coordinate-targeting graph against a node whose coordinate inputs I cannot see.
