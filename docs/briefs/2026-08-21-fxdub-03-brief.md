PASTE TARGET: the Comfy Cloud in-app agent (the "fx-dub v2" thread). Do NOT paste into any Claude/Grok session.

---

# fx-dub round 3 — v2 verified our side: strong mix bus, three blockers, one refuted claim (VALIDATE ONLY)

We pulled "fx-dub v2" over the API and verified it wire-by-wire. The mix bus is exactly what we ordered — AudioStandardize on both branches, AudioMix with primitive gains (VO 0 / bed −15), meters→SaveText on mix AND VO, SaveAudioAdvanced FLAC ×3, mux frame_rate wired from `source_fps`. Florence block is deterministic with the caption SaveText, tags come from an editable primitive, one shared seed. Good build. Your honest "no SRC node / no shot-onset node" answers are accepted and recorded — we'll measure the mix output rate ourselves at the audition run.

Now the corrections. Build these into a **NEW tab "fx-dub v2.1"** (version-bump on structural change; leave v2 untouched). Validate only — no runs.

## The refuted claim (please carry this correction)

You reported the ACE-Step 1.5 XL files "do not exist in the live node choices." **You checked `CheckpointLoaderSimple` only — diffusion models never appear in that list.** We inspected the loaders directly over the API:

- `UNETLoader` (Load Diffusion Model) HAS `acestep_v1.5_xl_base_bf16.safetensors` (plus `xl_sft`, `xl_turbo`, `1.5_turbo`, `1.5_base`)
- `VAELoader` HAS `ace_1.5_vae.safetensors`
- `CLIPLoader` HAS `qwen_0.6b_ace15.safetensors`, `qwen_1.7b_ace15.safetensors`, `qwen_4b_ace15.safetensors` — and its `type` COMBO includes `ace`

Better still: the measured reference graph is **on this workspace** — the tab/record "Motif builds v2", workflow_id `78a76ecd-7ae2-452a-afea-ad55a8d290f8`. Open it and **mirror its ACE loader stack and its negative-conditioning wiring exactly.** That graph is receipt-verified (29.7 gpu-sec confirmation run); it is the audit-ready configuration.

## Fix list for v2.1

- **F1 — BLOCKER, CFG is silently off.** Your KSampler's `negative` input is wired to the SAME conditioning output as `positive`. At cfg 6 the guidance algebra cancels (neg + cfg·(pos−neg) = pos) — you built a cfg-6 graph that behaves like cfg-1. Mirror the Motif-builds-v2 record's negative path.
- **F2 — BLOCKER, wrong model stack.** Replace the `ace_step_1.5_turbo_aio` checkpoint with the XL-base stack per the record: `UNETLoader acestep_v1.5_xl_base_bf16` + `VAELoader ace_1.5_vae` + the record's qwen CLIP loading (`type: ace`). Keep euler/simple/50 steps/cfg 6 + `ModelSamplingAuraFlow` shift 3 — that is the measured recipe for THIS stack. *(Optional, if cheap for you: a second tab "fx-dub v2.1-turbo" keeping your AIO checkpoint with the sampler block mirrored from the official ACE-Step 1.5 turbo template — read the template rather than guessing; we expect ~8 steps / cfg 1 there. That's the budget lane; it auditions separately.)*
- **F3 — BLOCKER, the dub loses its opening frames.** `skip_first_frames: 8` sits on the LOADER, and the same IMAGE output feeds `VHS_VideoCombine` — so the dubbed video is missing its first 8 frames. Set `skip_first_frames: 0`, keep `frame_load_cap: 0` (the mux needs the full stream), and cap Florence's input ONLY: insert an allowlisted frame subsampler between the loader IMAGE and `Florence2Run` (the VHS pack's Select Every Nth Image, or whatever the allowlist actually has — report the exact class you used; target ≈8 frames reaching Florence for a typical short clip).
- **F4** — `timesignature: "2"` → `"4"` (sane default; rhythm/modal color belongs in the tags prose).
- **F5** — duration: `source_duration` currently dangles and the bed length is a manual knob. If ANY float min/clamp/math node exists on the allowlist, wire `VHS_VideoInfo.source_duration` → clamp(≤60) → encoder `duration` + latent `seconds`, and drop the knob. If none exists, keep the knob and say "no float math on allowlist" explicitly.
- **F6 — keep:** `trim_to_audio: false`, h264-mp4 / yuv420p / crf 19, save_metadata true.

## Reply with

1. Validation state of "fx-dub v2.1" (and "-turbo" if built): error/warning counts, ready_to_run.
2. Disposition per F1–F6 (done / substituted-with-reason / blocked).
3. Exact node-class list including the subsampler and any float-math node.
4. Anything in the record's wiring you could NOT mirror (with the schema reason).

No generation runs — the Director auditions the configuration first. After your reply we pull v2.1, verify, and then the Director orders the audition run (pinned seeds, one clip).
