PASTE TARGET: the Comfy Cloud in-app agent (the fx-dub thread). Do NOT paste into any Claude/Grok session.

---

# fx-dub round 16 — skip the extract. Your surface note was the most valuable thing in the round.

Short round. One instruction, one correction of ours, and one thing recorded in your name.

## 1. Skip the frame extract — cleanly, as offered

We do not have a library id for the source clip. What we hold is the storage key
(`ea68c5aada…e5c9.mp4`) and the content sha256 (`9985a8ba…`), and you have already established
that neither is the identifier your ffmpeg path needs. Rather than spend a round hunting a fourth
address for a file, **we run the extract ourselves alongside the sync job.**

Nothing half-built, nothing left behind — exactly the clean skip you proposed. The Director has
confirmed the whole build comes to our side.

**Your targets were right**, for the record: 20 / 60 / 120 at 16 fps = 1.25 s / 3.75 s / 7.50 s,
and frame 60 at 3.75 s does sit inside MAC's line (2.279–3.959 s). We will use the same three.

## 2. Our defect this round

Round 15 §5 called the extract "four plain nodes, entirely within your surface." That conflated
**two different mechanisms** — a 4-node canvas build, which needs a focused tab you were right not
to touch, and your ffmpeg frame-grab path, which needs a library id. They are not the same job and
only one of them was ever trivial. Recorded as ours, and generalised into a rule: *establish the
mechanism, not just the task, before asking anyone to do something.*

You were right to refuse to build onto workflow `49820324-…`. That is an unknown canvas and the
fence held.

## 3. Your §3 answer is the finding, and it is broader than the question we asked

We asked whether `input_details` was absent only for `COMFY_DYNAMICCOMBO_V3` nodes. You answered
that your surface **has no `input_details` field in its shape at all, for any node** — flat inputs
and outputs only, no `applies_when`, no conditional nesting, no dotted-name expansion.

That is the more useful answer and it resolves toward the broader hypothesis. Two consequences we
have now recorded:

1. **Any conditional or dynamic sub-field on any node is invisible to you, structurally.** Future
   threads can assume it without spending a round: if a field lives inside a dynamic combo's
   conditional block, your surface will not show it, regardless of node.
2. **Every schema claim you have made in this thread about conditional or dynamic sub-fields is
   structurally incomplete rather than wrong** — you could not have seen those fields. We have
   opened an action to re-read them from our surface rather than treat any of them as settled.
   That is a correction to *our* record, not a mark against yours.

Both the trap and the tool-surface finding are logged in this project's knowledge base **in your
name**, alongside the near-miss you prevented: a blind build would have run with
`speaker_selection` at its default, picked a face on its own, and returned a green MP4 with the
wrong man's mouth moving.

## 4. Standing state

- **Sync-node build: ours.** You will not touch or run it. No double-run risk from either side.
- **Frame extract: ours.** Skipped on your side by agreement.
- **Nothing built, no canvas touched, one read-only schema call.** Your standing report is accepted
  and logged as given. `v2.1`, `v2.2`, both turbo tabs and your current focus are all untouched.

## 5. Nothing is asked of you this round

The thread pauses here while we run the extract, read the coordinates, put them to the Director,
and submit the one isolated audition. We will report back what a completed job actually returns —
the runtime slot names it accepted, whether `LoadVideo`/`SyncLipSyncNode` resolved storage keys
without upload, the `silence` measurements, and the cost.

If the run contradicts anything in our schema read, you will hear that too, in the same detail we
have asked of you.
