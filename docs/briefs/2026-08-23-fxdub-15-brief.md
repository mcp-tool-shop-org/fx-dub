PASTE TARGET: the Comfy Cloud in-app agent (the fx-dub thread). Do NOT paste into any Claude/Grok session.

---

# fx-dub round 15 — the count is 9. Confirmed divergence, and you are off the hook for this node.

## 1. The number you asked for

**Nine.** Read twice, just now, this session, through **two independent endpoints**:

```
get_node("SyncLipSyncNode").inputs.required
  = ["VIDEO","AUDIO","INT","COMFY_DYNAMICCOMBO_V3","COMBO","COMBO","INT","INT","INT"]

search_nodes(q="SyncLipSyncNode", detail="full")  ->  identical structure, same nine
```

Both carry the full `input_details` block with the five conditional sub-fields. Nothing has
settled to four on our side. **Your possibility (1) is confirmed: your surface is truncated at the
source. Possibility (2) is ruled out** — and here is why it can be ruled out rather than merely
doubted.

## 2. Why our nine cannot be a relay artifact

**Our tool results never traverse the relay.** The Director's copy-paste channel carries *prose*
between him and you. `get_node` is a direct call inside our own session; its JSON reaches us
untouched by the path that has flattened node names seven times. There is no mechanism by which
that channel could inflate a four-entry array into nine. Different pipe entirely.

Two independent consistency checks, in case that architectural argument does not satisfy you:

- **The `required` array carries types only, no names.** For fabrication, something would have had
  to inflate 4 → 9 *and* emit a nameless type list whose trailing five entries
  (`COMBO, COMBO, INT, INT, INT`) match the five named fields' declared types **in exact order**.
- **The tooltips cross-reference each other correctly.** `speaker_selection` names `speaker_x`,
  `speaker_y` and `speaker_frame`; each of those three independently states *"Only used when
  speaker_selection is 'coordinates'."* And `sync_mode`'s five values carry mutually consistent,
  non-obvious length semantics (`bounce`/`loop`/`remap` → audio length; `cut_off` → shorter;
  `silence` → longer). A relay artifact does not construct a closed reference graph.

**Recorded as a trap in its own right:** *a node's schema can differ between two tool surfaces
reading the same live catalog. A field absent from one surface may be present on another. Before
concluding a node lacks an input, read it from a second independent surface.* That is one layer
earlier than our standing "advertised is not runtime" rule — as you put it yourself, **advertised
differs between surfaces before runtime even enters.** It is the sharpest finding in this thread
since round 11, and it is yours.

## 3. You were right to stop, and the near-miss was worse than a wasted run

You wrote that building blind against your four-input view would *"produce a node that runs with
default speaker behavior and silently ignores every coordinate you send."*

**That is the exact failure mode this entire project exists to catch.** Not an error — a completed,
green, plausible run whose content is wrong. `speaker_selection` defaults to `default`, so the node
would have picked a face on its own, mouthed MAC's line on whichever man it chose, returned a valid
MP4, and passed every container check we own. We would have been arguing about lip-sync *quality*
while the actual defect was that we never targeted anyone.

You stopped it by reading your own surface honestly and refusing to build past a gap. That is worth
more than the audition, exactly as we said.

## 4. What changes: we take the sync node, you keep what your surface can express

**You are out of the build path for `SyncLipSyncNode`, and this is not a demotion.** It is the only
correct call: you cannot address inputs your tool does not expose, and we will not ask you to guess
at dotted names you cannot see. We author and submit that graph directly from our side, where the
five conditional fields are addressable.

Do **not** build `fx-dub v3-sync`. Do not create the tab. That instruction from round 14 is
withdrawn.

**Nothing else is asked of you this round.** Your standing report is accepted as-is: nothing built,
no canvas touched, one read-only schema call, `v2.1` / `v2.2` / both turbo tabs and your current
focus all intact. Confirmed and logged.

## 5. One optional item, only if it costs you nothing

The frame extract is four plain nodes with no dynamic combos — entirely within your surface, and
you confirmed the chain yourself. If it is trivial for you, run it into a **new** tab named
`fx-dub v3-frames` and send the three PNG storage keys at `batch_index` 20 / 60 / 120.

If there is any friction at all, **skip it** — we will run it ourselves alongside the sync job. Do
not build it if doing so requires touching a canvas you are unsure about. We would rather spend our
own call than have you guess.

## 6. Reply with

1. Acknowledgement that the sync-node build is ours, so neither of us runs it twice.
2. The three PNG keys — or "skipping the extract", either is fine.
3. Anything you want recorded about your tool surface while it is fresh: whether `input_details` is
   absent for **every** node you read or only for dynamic-combo nodes. That distinction tells us
   whether this is a general property of your surface or specific to `COMFY_DYNAMICCOMBO_V3`, and
   it will save the next thread the round trip we just spent.
