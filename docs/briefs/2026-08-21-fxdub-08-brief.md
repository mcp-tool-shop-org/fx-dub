PASTE TARGET: the Comfy Cloud in-app agent (the fx-dub thread). Do NOT paste into any Claude/Grok session.

---

# fx-dub round 8 — Director says build the turbo tab (VALIDATE ONLY; audition still pending)

The Director has green-lit the second tab you offered in round 4. This is independent of the audition, so you can build it while we wait on the clip upload. **The filename instruction is still pending and unchanged — do not touch either tab's loader `video` widget.**

## Why this tab exists — it is a controlled A/B, not a variant

The point is a **cost/quality comparison on identical inputs**: XL-base at 50 steps / cfg 6 versus the turbo AIO at its own recipe. That only means anything if *everything except the model and its sampler settings is held constant*. So:

**Build a NEW tab `fx-dub v2.1-turbo` as an exact copy of `fx-dub v2.1`, changing only the generator block.**

Hold constant (copy verbatim, do not "improve"):
- the shared `PrimitiveInt` seed = 1 driving both encoder and sampler
- the tags primitive (same text), the dialogue primitive (same line), duration primitive = 10
- Florence block: `Florence-2-large` / fp16 / sdpa / `more_detailed_caption` / `do_sample=false` / seed 1
- `VHS_SelectEveryNthImage(30)` on the Florence branch only; loader `skip_first_frames=0`, `frame_load_cap=0`
- the whole mix bus: `AudioStandardize`×2 → `AudioMix` (VO 0 dB / bed −15 dB primitives) → 2 meters → 2 `SaveText` → 3 `SaveAudioAdvanced` FLAC
- `VHS_VideoCombine` h264-mp4 with `frame_rate` ← the `PrimitiveFloat` 16.0 pin
- **change the `filename_prefix` values only**, to `fxdubturbo/...` (mix, stem_bed, stem_vo, caption, mix_lufs, vo_lufs, dubbed) so the two runs' artifacts never collide in the output folder. This one is required — same-prefix collisions would make the A/B unreadable.

Change only this:
- Replace `UNETLoader` + `VAELoader` + `DualCLIPLoader` with `CheckpointLoaderSimple` → `ace_step_1.5_turbo_aio.safetensors` (MODEL/CLIP/VAE from the one node), feeding the same `TextEncodeAceStepAudio1.5`, `ConditioningZeroOut`, `EmptyAceStep1.5LatentAudio`, and `VAEDecodeAudio`.
- Set the sampler block to whatever the **official ACE-Step 1.5 turbo template** specifies. **Read that template and mirror it — do not guess and do not carry the XL recipe over.** We expect roughly 8 steps / cfg 1 and possibly a different `ModelSamplingAuraFlow` shift, but the template is the authority, not our expectation.

## Report back (this is a measurement we want regardless of the audition)

1. **Exactly what the official turbo template specifies**: sampler_name, scheduler, steps, cfg, denoise, and the AuraFlow shift — quoted from the template, with the template's name/id.
2. Validation state of `fx-dub v2.1-turbo` (errors/warnings/ready_to_run/node count).
3. Anything you could not hold constant, with the reason.
4. Confirmation that `fx-dub v2.1` is untouched and that neither tab's loader `video` widget was set.

**No runs.** Both tabs get auditioned together on the same clip once it is uploaded, so the Director hears the same scene twice and the credit difference is measured, not estimated.
