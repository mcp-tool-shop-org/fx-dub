# fx-dub round 11 — REPLY (Comfy Cloud in-app agent)

> ## ⛔ CORRECTION BANNER — READ BEFORE USING ANYTHING BELOW
>
> **This reply's headline answer is WRONG, and it is the kind of wrong that
> deletes a working capability.** It states that `LoadAudio` cannot feed
> `ElevenLabsInstantVoiceClone`, calls that "a hard, type-level fact," and
> **recommends dropping voice cloning entirely.**
>
> Voice cloning works. It was proved by execution the same day:
> **job `920dc2e0-e420-473a-9cb9-45b84b0fea65`, status `completed`**, producing
> a 48 kHz / 3.520 s FLAC from
> `LoadAudio → ElevenLabsInstantVoiceClone → ElevenLabsTextToSpeech`.
>
> The runtime slot name is **`files.audio0`**. See
> `2026-08-22-fxdub-11-verification.md` for the refutation in full.
>
> This file is preserved unedited-in-substance because the archive is the
> studio's record and deleting a wrong answer destroys the evidence of *how*
> it was wrong. Do not act on it.

**Date:** 2026-08-22 · **Direction:** inbound · **Round:** 11
**Brief:** `2026-08-22-fxdub-11-brief.md`
**Verification:** `2026-08-22-fxdub-11-verification.md`

---

## What was asked

A measured survey of the entire TTS surface on Comfy Cloud: every voice node with
its rate and selection mechanism, full preset rosters per engine, the
delivery-knob map, whether `eleven_v3` inline audio tags actually work when
tested, and — flagged in the brief as *"the cloning question is the important
one"* — a definitive yes/no on whether an AUDIO link from `LoadAudio` feeds
`ElevenLabsInstantVoiceClone` / `FishAudioInstantVoiceClone`, with the API-format
shape. Plus six auditioned deep/gritty candidates across three engines.

## What came back

### Item 4 — the cloning question ❌ WRONG

Verbatim:

> "**DEFINITIVE: No.** You cannot wire LoadAudio into the ElevenLabs clone node.
> This is now a hard, type-level fact, not an opinion:
> `ElevenLabsInstantVoiceClone.files` accepts type `COMFY_AUTOGROW_V3` (a panel
> file-picker), not AUDIO. […] returns 0 producers — nothing in the catalog emits
> `COMFY_AUTOGROW_V3`. There is no bridge node. […] **Recommend dropping the
> clone route entirely.**"

**The reasoning error:** it searched for *what node outputs type
`COMFY_AUTOGROW_V3`*, found zero producers, and concluded the input cannot be fed.
But `COMFY_AUTOGROW_V3` is a **container declaration, not a wire type**. The slots
it grows accept ordinary AUDIO links.

This is the **third instance of the same trap class** in this thread — after the
`CheckpointLoaderSimple` "files not installed" claim (round 2) and our own
`LoadVideo` COMBO "audition blocked" claim (round 9). In round 9 *we* made it.
**Absence of evidence in one enumeration is not evidence of absence.**

### Item 1 — engine inventory ❌ PARTIALLY WRONG

> "**Fish Audio — not on platform** (search returns only HeyGen). MiniMax — no
> speech node."

Fish Audio **is** on the platform: four nodes under `partner/audio`
(`FishAudioTextToSpeech`, `FishAudioVoiceSelector`, `FishAudioInstantVoiceClone`,
`FishAudioSpeechToText`). `FishAudioTextToSpeech` was subsequently *executed*.

The MiniMax half is plausible and was not contradicted.

### Item 2 — preset rosters ⚠ UNDER-REPORTED

Named two ElevenLabs voices (Brian, Bill) and two HeyGen voices (Chill Brian,
Thanos). The brief asked for the **full** roster per engine; the live rosters are
ElevenLabs 21, HeyGen 67, Fish Audio 17 + custom voice ID, ByteDance 40.

### Item 3 — delivery-knob map ✅ SOUND, with an honest gap

The knob table is broadly correct. Credit where due — it explicitly declined to
claim the audio-tag result it had not tested:

> "One correction on the `eleven_v3` audio-tag test: I did not run an isolated
> `[whisper]`-tag probe this turn. […] I'm flagging it rather than claiming it."

That is the right behaviour, and it is the fourth honest gap this agent has
declared across the thread.

### Item 5 — six auditions ❌ NOT DELIVERED

No new renders. The agent stated so plainly:

> "Nothing was run this turn — the pitch render already existed, so I probed it
> rather than re-spend."

Its "tested, not quoted" ByteDance evidence — `fxvox6/bytedance_scene_pitch-6`,
48 kHz stereo, 9.84 s — is **our own artifact**, produced advisor-side earlier the
same session. Correctly identified; not independently generated.

## Scoreboard

| | |
|---|---|
| Agent defects this round | **3** (a false impossibility with a "drop it" recommendation; Fish Audio absent; rosters under-reported) |
| Agent honest-gap declarations | 1 (audio tags) — its 4th across the thread |
| Items delivered as briefed | 1 of 5 |

This is the first round in the thread where the agent's error count exceeded ours,
and the first where a recommendation would have removed a working capability. Its
prior record — four consecutive rounds of correctly declared gaps, and being right
against us in round 9 — remains intact and still argues for weighing its reports
seriously. **Verify, don't relay** is not *assume wrong*; it is *check*.
