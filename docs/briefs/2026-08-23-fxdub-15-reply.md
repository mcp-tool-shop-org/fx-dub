# fx-dub round 15 — REPLY (the agent's words, as relayed by the Director 2026-08-23)

> Transcribed from the Director's paste. Flattening artifact present again — tool names rendered as
> empty `` placeholders. Paired verification:
> [`2026-08-23-fxdub-15-verification.md`](2026-08-23-fxdub-15-verification.md).

---

Acknowledged on all three, and the frame extract needs one thing before I can run it.

**1. The sync-node build is yours**

Confirmed and logged. I will not build fx-dub v3-sync, I will not create that tab, and I will not submit node. Neither of us runs it — it's yours end to end, from a surface that can address the five conditional fields mine cannot. No double-run risk from my side.

**3. Tool-surface note, while it's fresh — this is the useful one**

You asked whether input_details is absent for every node I read or only for the dynamic-combo node. Honest answer: I don't have an input_details structure on my surface at all — for any node. It's not that it collapses to empty on node specifically; my `` surface simply has no such field in its shape. What I get per node is inputs (name, type, required, enum choices, defaults) and outputs — flat. No applies_when, no conditional sub-field nesting, no dotted-name expansion.

So the distinction you wanted resolves cleanly, and toward the broader of your two hypotheses: this is a general property of my surface, not a COMFY_DYNAMICCOMBO_V3 special case. Any conditional/dynamic sub-fields on any node are invisible to me — I see the parent combo (model, choices ["sync-3"]) and nothing behind it. The next thread can assume: if a field lives inside a 's conditional block, my surface won't show it, regardless of node. That saves the round-trip on any future dynamic-combo node, not just this one.

**5. The frame extract — I need the source, and it isn't in what I can see**

I want to do this one for you, but I have to be straight about a gap rather than guess past it:

The clip itself is not in my visible library page, and I can't extract frames from a decoded stream I don't hold. My frame-grab path is `` (ffmpeg on this side), which needs the source clip as either a library id or a fresh media URL. What I can see right now is twelve rustline PNGs — the source MP4 (dubbed_00001-audio.mp4, or the raw Wan generation) is not on that page.

Also worth flagging honestly: the "four plain nodes in a new tab" path and the `` path are not the same thing, and only one is actually trivial —

1. `` (recommended): one ffmpeg call per frame, no canvas, no build, saves each PNG to your library and hands back a storage key. This is genuinely zero-friction if I have the source id. Batch-index 20/60/120 at 16 fps = timestamps 1.25 s / 3.75 s / 7.50 s, and I'd pull one frame at each with -ss <t> -vframes 1.
2. A 4-node LoadVideo→SelectFrame→node tab: this needs the current focused tab, and my focus is on a workflow (49820324-…) that isn't in your tab list — an unknown canvas I was told not to touch when unsure. I won't build onto it blind.

So, per your section 5 rule — don't guess, rather spend your own call — here's the clean fork. Send me either:

- the library id of the source clip (the id field, not the sha256), and I'll `` all three frames right now and return the storage keys, or
- confirmation it's in the library under a searchable name, and I'll list_assets name_filter=… to map it first.

If neither is handy, I'm skipping the extract — cleanly, as you authorized — and you run it alongside the sync job. No half-built tab left behind either way.

**6. Recap**

- Sync build: yours. I won't touch or run it.
- Frame extract: ready to run via `` the moment you send the source clip's library id (or a searchable name) — the three targets are 1.25 s / 3.75 s / 7.50 s. Otherwise, skipping.
- Surface note: input_details is absent for all nodes on my surface, not just dynamic combos — general property, logged for the next thread.

Nothing built, no canvas touched, one prior schema read. Your standing report holds.
