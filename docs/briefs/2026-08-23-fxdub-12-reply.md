# fx-dub round 12 — REPLY (the agent's words, as relayed by the Director 2026-08-23)

> Transcribed verbatim from the Director's paste. **Note the flattening artifact is present in
> this message too** — see the `` `node` `` placeholders below, which appear where node class names
> should be. Paired verification: [`2026-08-23-fxdub-12-verification.md`](2026-08-23-fxdub-12-verification.md).

---

I'm going to stop before building anything, because several of your requests rest on a premise I have to correct honestly rather than perform around.

The "one reason" — I can't send it, because I never wrote it. There is no truncated sentence I'm holding back. I have no record in this thread of a prior reply from me naming your subject as "the hard case for exactly one reason," and I won't reconstruct a sentence I didn't write and then present it as the one you lost. Similarly, the five node-class names that "arrived as the literal string node" did not come from me this turn. I can't authenticate messages attributed to a previous me. The honest position: treat nothing attributed to my prior self as load-bearing until I've re-derived it live. That's the opposite of a defect I can patch — it's a boundary I have to state.

Now the parts I genuinely can and can't do:

**What I cannot verify or do this turn — stated plainly, not guessed:**

MAC's face coordinates off a decoded frame (item 5). I have no visual grounding. I cannot decode your H.264, I cannot see the two men, I cannot tell you which is MAC or read x/y off any frame. Anything I gave you here would be fabricated. This one is not "genuinely mine" — it's genuinely impossible for me. You need a frame extracted (your rig has no ffmpeg, but `` on this side could pull a frame if the clip is in the asset library) and then a human or a vision model to locate the face. I can extract the frame for you if you point me at the asset; I cannot annotate it.

The runtime slot names and the exact API-inputs block from a completed job (item 4). I can read a node's advertised schema with a tool. I cannot hand you the block "as the runtime accepts it" from a completed job unless such a job exists and I fetch it — and I have no completed node job to read. Your round-11 lesson (advertised files.item_1 vs actual files.audio0) is exactly why I won't transcribe the advertised names and call them runtime-proven. I'd be repeating the error you're warning me about.

VIDEO-by-storage-key, execution-proven (item 3). You've explicitly said a dry_run PASS is not an acceptable answer, and only a completed job proves it. I have no completed job proving VIDEO resolves by storage key the way LoadAudio did. So the honest answer is: unproven. I will not assert yes or no. It can only be settled by the audition run actually resolving a VIDEO storage key without upload — which is itself one of the things the run would demonstrate.

**What I can actually verify right now, if you want me to proceed:**

Confirm/refute the Kling disqualification (item 2) — by reading `node`'s real schema and checking your 480px / 10.0625s measurements against its stated bounds. That's a live tool read, not a memory claim.

Read `node`'s real schema — confirm sync_mode has a silence value, confirm speaker_selection accepts coordinates, and give you the advertised slot names clearly labelled as advertised, not runtime-proven.

Build the isolated audition graph and run it (item 7): MAC's per-character stem → `node`.audio, sync_mode: silence, coordinate speaker selection, in its own job. From a completed run I can then give you the real silence measurements (item 6) and the credit/gpu cost.

**But that build is blocked on two inputs only you can resolve:**

MAC's stem. You describe fxdub-dialogue MAC producing it on your side. There is no cloud audio-attachment path — an uploaded audio file is not visible to a LoadAudio node here, and I won't point one at a filename that can't resolve. The stem has to either already exist as a resolvable cloud storage key (like your round-11 LoadAudio case) or be generated in-graph. Which is it?

MAC's coordinates. The run needs them and I cannot produce them (above).

One question, since the whole audition hinges on it: is MAC's per-character stem already a resolvable cloud storage key (the way your round-11 LoadAudio resolved a key its never listed), and can you give me the face coordinates — or do you want me to first extract a frame from the clip's library asset so a human can read them off?

Answer those and I'll read both schemas, build the isolated Sync job, and run the single audition — reporting only what a completed job actually returns.
