PASTE TARGET: the Comfy Cloud in-app agent (the fx-dub thread). Do NOT paste into any Claude/Grok session.

---

# fx-dub round 6 — GO on the frame_rate pin; clip upload handled our side (STILL VALIDATE ONLY)

Good answers on both, and thank you for saying plainly what you could not verify rather than filling the gap.

## Q2 — YES, pin it. Make this one wiring change.

**Order: replace the `VHS_VideoInfo.source_fps` → `VHS_VideoCombine.frame_rate` link with a new `PrimitiveFloat` = 16.0.** Your reasoning is right and we're taking it. Two independent measurements now agree on the fixture:

- your ffprobe read: `avg_frame_rate 16/1`, `r_frame_rate 16/1`, `nb_frames 161`, duration 10.0625 s, `time_base 1/16384`
- our local MP4 atom probe: 161 frames, `mdhd` 16384/164864 = 10.062 s, `avc1`, one video track, no audio

Note these do **not** conflict with the malformed-header finding — they're different fields. ffprobe read the **track**; we read the **movie header** (`mvhd`), where the timescale is 0. Both readings are correct, which is exactly why pinning wins: the track-level rate is provably 16, the movie-level rate is provably garbage, and nobody knows which one VHS's derivation reads. A primitive removes the question instead of betting on it.

Keep `VHS_VideoInfo` in the graph (its `source_duration`/`source_fps` outputs stay available for the future duration-coupling work); just don't let `frame_rate` depend on it for this audition. Please note in your reply that the pin is fixture-specific — a general-purpose fx-dub run on arbitrary footage should go back to `source_fps` once we've measured that it derives correctly.

## Q1 — no upload needed from you; we've confirmed the boundary and will handle it

We tested the API path from our side: the MCP's file-upload tool is **image-only** (`.jpg/.jpeg/.png/.webp/.gif`) and rejects `.mp4` outright. So your "no in-graph path, and no upload path of my own" answer is correct and now confirmed from the other direction too — **there is no video-into-inputs route over the API at all**, in either of our toolsets. The Director will upload the clip through the browser (the loader's own upload control).

Please **skip the asset-library remux** — you're right that it doesn't solve Q1, and every extra job on that clip costs real credits for no gain.

The file going up is byte-identical to what we verified: `sha256 9985a8ba6197ea7c02adc99c4c3aafc2a9d1cfa13e535ede64908ebea327a30c`, 963,326 bytes. We'll send you the exact filename as it appears in the loader list once it's up — **do not guess it**, and do not change the loader's `video` widget until you have that name from us.

## Everything else stays frozen

Florence back to `more_detailed_caption` ✔ (thank you — and the "widget values, not just topology" lesson is now a written rule on our side, not just a note). `select_every_nth=30` → ~6 frames ✔. Mix bus, XL stack, ConditioningZeroOut negative, 1.5 latent, subsampler placement: all verified clean by us and unchanged.

## Reply with

1. Confirmation the `frame_rate` pin is wired (PrimitiveFloat 16.0) + the tab's validation state.
2. Confirmation you did **not** run anything and did **not** touch the loader's `video` widget.

Then we upload, send you the filename, you point the loader at it and re-validate — and **only then** does the Director order the audition run.
