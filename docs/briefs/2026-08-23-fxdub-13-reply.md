# fx-dub round 13 - REPLY (the agent's words, as relayed by the Director 2026-08-23)

> Transcribed from the Director's paste. The flattening artifact is present again - see the empty
> `` placeholders and `LoadVideo`'s type rendered as `****`. Paired verification:
> [`2026-08-23-fxdub-13-verification.md`](2026-08-23-fxdub-13-verification.md).

---

Several findings land here, and two of them contradict things in your brief. Let me report exactly what the live catalog returned, and where I can't get you what you asked.

**1. The two MAC "durations" - I can't deliver them, and the request path is malformed**

37d38cda... and d7ba748c... are sha256 storage keys, not job ids. ``/get_job take a run's prompt_id; handed a storage hash they return HTTP 400, which is what happened. So "call on both MAC keys" cannot execute as written - not a refusal, a type mismatch.

The correct read for a stored asset's duration is ``, which needs the library id (not the hash). I pulled your library to map hash->id, but the listing came back elided and the head shows only the rustline PNGs - neither MAC key is in the visible page. Before I can probe either, I need one of:

- the library id for each MAC key (the id field, not the sha256), or
- confirmation they're in the library under a searchable name so I can list_assets name_filter=... and map them.

If you have the ids, send them and I'll probe both and report durations with no job. That's the the model-1.7s test you want, and it still costs nothing - I just need the right identifier.

**2. Kling's live schema - your recorded bounds are NOT in it (correction, item 4)**

You asked me to confirm node states 720-1920 px and 2-10 s. The live schema states neither. Its full input set right now is:

- video (VIDEO, required)
- audio (AUDIO, required)
- voice_language -, choices ["zh","en"], required

There are no height/width/length widgets on this node, and therefore no numeric bounds to read. The 720-1920 / 2-10 s limits are real Kling API constraints, but they are not surfaced as node schema - they'd fail server-side at run, not at validation. So I can't "confirm from the live schema" what the schema doesn't carry. Your disqualification of Kling still holds on the underlying API limits; I just won't claim the node advertises them. Treat this as a correction to item 4's premise.

**3. node's live schema - also differs from your round-12 baseline (flag for you)**

You gave advertised names model.sync_mode, speaker_selection, speaker_frame, speaker_x, speaker_y under model = COMFY_DYNAMICCOMBO_V3 / sync-3. The live node shows a much smaller surface:

- video (VIDEO, required)
- audio (AUDIO, required)
- seed (INT, required)
- model - COMFY_DYNAMICCOMBO_V3, choices ["sync-3"], required

No sync_mode, no speaker_*, no speaker_x/y at the top level. Those may be nested behind the (they resolve after model is chosen - that's what COMFY_DYNAMICCOMBO_V3 does), which is consistent with your round-11 lesson that advertised != runtime. But as read cold, this node exposes no coordinate inputs. Which means the whole frame-extract -> speaker_x/speaker_y plan may target inputs that don't exist on this node - or exist only after the expands. I can't tell you which from a static read. This is the thing in section 4 I think may be wrong, and you asked me to say so before a paid run.

**4. LoadVideo confirms your Class-A prior**

LoadVideo live: single input file, type **** - and consistent with an empty/unlisted options set that still resolves an arbitrary key. Matches round 9. Good to go for the extract.

**5. The frame-extract graph - I can build it, one correction to the node chain**

GetVideoComponents live outputs images, audio, fps, bit_depth (your graph's ["2",0] = images OK). ImageFromBatch takes image, batch_index, length OK. The four-node decode graph is sound and I can build the fan-out (one GetVideoComponents -> three ImageFromBatch->node at 20/60/120). That run is decode-only, no inference, cheap.

Before I build or run anything, two blockers and one decision:

- MAC durations: send the two library ids (or confirm they're searchable) - the sha256 keys can't be probed directly.
- The coordinate question is real: node as read exposes no speaker_x/y/frame. Do you want me to (a) build the frame-extract now regardless - it's cheap and you want the PNGs either way - while we work out whether the coordinate inputs materialize under the, or (b) hold until we've resolved where those inputs live, since extracting frames to read coordinates is pointless if the node has nowhere to put them?

My recommendation: build and run the frame-extract now (a is cheap, decode-only, and the PNGs are useful independent of the coordinate question), and separately I'll expand the model to see if the speaker inputs appear. But I'm not modifying any archived tab, and I'm currently focused on a tab that isn't in your list - so tell me: new tab for the extract, or a specific tab? I won't guess which canvas to touch given how carefully you've fenced the archives.
