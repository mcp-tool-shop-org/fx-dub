PASTE TARGET: the Comfy Cloud in-app agent (the fx-dub thread). Do NOT paste into any Claude/Grok session.

---

# fx-dub round 14 — two of your corrections are right and improve the work. One is wrong, and here is the schema.

## 0. Scoreboard for round 13: our defects 2, yours 1

| # | Finding | Whose |
|---|---|---|
| 1 | `get_output` on a storage hash is a **type mismatch**, not a refusal — it takes a `prompt_id` | **Ours.** Accepted and recorded. |
| 2 | Kling's 720–1920 px / 2–10 s bounds are **not numeric schema bounds** | **Ours**, half — we told you to "confirm from the live schema" what lives in prose. Your refinement is better than what we had. See section 3. |
| 3 | *"`SyncLipSyncNode` exposes no `speaker_x/y/frame`… the whole frame-extract plan may target inputs that don't exist"* | **Yours.** Refuted below, live, this morning. |

You asked us to tell you before a paid run. Doing that now.

## 1. REFUTED — the coordinate inputs exist. Full schema, pulled live

`get_node("SyncLipSyncNode")`, this morning. Start with the required-type array, because it settles
the count before any name is read:

```
inputs.required = ["VIDEO","AUDIO","INT","COMFY_DYNAMICCOMBO_V3","COMBO","COMBO","INT","INT","INT"]
```

**Nine entries.** The four you named consume `VIDEO, AUDIO, INT, COMFY_DYNAMICCOMBO_V3`. The
remaining **`COMBO, COMBO, INT, INT, INT`** are exactly `sync_mode`, `speaker_selection`,
`speaker_frame`, `speaker_x`, `speaker_y` — in that order. They are not absent; a lean view
collapsed them.

Each arrives in `input_details` marked `"conditional": true, "applies_when": ["sync-3"]`:

| Field | Type | Default | Options / range |
|---|---|---|---|
| `model.sync_mode` | COMBO | `bounce` | `bounce`, `cut_off`, `loop`, `silence`, `remap` |
| `model.speaker_selection` | COMBO | `default` | `default`, `auto-detect`, `coordinates` |
| `model.speaker_frame` | INT | 0 | 0 – 1000000 |
| `model.speaker_x` | INT | 0 | 0 – 4096 |
| `model.speaker_y` | INT | 0 | 0 – 4096 |

And the `model` field carries a `hint` that states the mechanism you correctly guessed at:

> *"Dynamic combo: pick one `options` value for `model`, then send each conditional sub-field below
> using its full dotted name (prefixed with `model.`, e.g. `model.<sub_field>`). Auto-grow list
> slots nest one level deeper. Sending these fields flat (without the prefix) fails with
> `required_input_missing`."*

The `speaker_selection` tooltip, verbatim, so there is no doubt we are reading the same node:

> *"Which face to lipsync when several people are visible. default: let the model decide.
> auto-detect: detect and follow the active speaker. coordinates: target the face at pixel
> (speaker_x, speaker_y) in the frame chosen by speaker_frame."*

**`sync-3` is the only option `model` accepts**, so the conditionals are not merely reachable —
they always apply. Your instinct ("they may be nested behind the dynamic combo") was right; the
conclusion drawn from it ("as read cold, this node exposes no coordinate inputs") was not.

**Therefore the frame-extract plan targets inputs that exist, and your option (b) — hold — is
void.** Your option (a) was correct for its own reasons. Take it.

One thing your read does confirm and we should both hold: **advertised is still not runtime.**
Round 11 precedent stands (`files.item_1` advertised, `files.audio0` actual). We are not asking
you to trust these names — we are asking you to send them and report what the runtime accepted.

## 2. Blocker 1 is dissolved, our side, with no probe and no ids

Stop trying to read durations from the cloud. **We measured it locally instead**, which we should
have done before asking you:

| Artifact | Measured |
|---|---|
| `mac_onframe_pitch0.flac` (MAC's ByteDance render) | **4.032 s**, 48 kHz, stereo |
| MAC's isolated content | 1.72 s |
| MAC's speaking window in the shipped scene | 2.279 s → 3.959 s |

`4.032 − 1.72 = 2.312 s` of leading material, against a measured onset of **2.279 s**. That closes
it: **the ByteDance render already carries the leading silence written into the file.** Our
recorded absolute-timeline behaviour holds, and MAC's render *is* a correctly positioned stem. No
assembly, no `EmptyAudio`, no `AudioConcat`, and certainly no `AudioPad`.

**Use `d7ba748c5359d0df8119e41df13dbaf725fb1bc88271806ceac69055b53a1b55`** — the take that shipped.
If it resolves and plays, it is right. Its exact length does not matter: `sync_mode: silence` pads
the shorter track, so anything from 4.032 s to 10.0625 s produces the same 161-frame output with
MAC's mouth moving at 2.279 s and closed elsewhere — which is the correct performance, since MAC is
listening through VOICE's three lines.

**Do not send us library ids and do not go hunting for them.** The question they were meant to
answer is answered.

## 3. Kling — your correction accepted, and it upgrades our record

You are right that the node exposes no height/width/length widgets, so there are no numeric bounds
to read. Where we got the numbers: the node's own **description string** carries them in prose —

> *"The audio file should not be larger than 5MB. The video file should not be larger than 100MB,
> should have height/width between 720px and 1920px, and should be between 2s and 10s in length."*

So both readings are correct and they matter differently. **Your consequence is the important
one:** those limits are enforced server-side at run, not at validation. A graph that violates them
**validates clean and then fails after the credits are committed.**

We are recording that as a trap in its own right — *a partner node's operating constraints can live
only in its description text; the validator cannot see them* — and it is a sibling of the rule we
already hold, that a `dry_run` PASS proves nothing. Good catch. Kling stays out on the underlying
API limits; we will not say the node advertises them.

## 4. Your two questions, answered

**Which option — (a) or (b)?** **(a), your recommendation.** Build and run the frame extract now.
It is unblocked on both fronts: the coordinate inputs exist (section 1), and the stem question is
closed (section 2).

**Which tab?** **A new one. Name it `fx-dub v3-sync`.**

Never the focused tab, and never an archive. In round 3 an `open_workflow` on a reference replaced
the focused tab's canvas **and clobbered its saved server-side file**; we recovered only because we
had the graph archived our side. The standing rule since: open references into a new tab, or read
them over the API. You were right not to guess which canvas to touch — that fence is real and it
was paid for.

`fx-dub v2.1`, `v2.2` and both turbo tabs remain untouchable.

## 5. Then the audition — everything except the coordinates is pinned now

Do **not** run this until we send `speaker_x` / `speaker_y`. Pinning the rest so nothing is left to
inference:

```
video                     <- LoadVideo, file = ea68c5aada3b35b0c8be343f52671cf40ef30066b7d2852766ffe86f1292e5c9.mp4
audio                     <- LoadAudio, key = d7ba748c5359d0df8119e41df13dbaf725fb1bc88271806ceac69055b53a1b55
seed                      = 42
model                     = "sync-3"
model.sync_mode           = "silence"          <- the only mode that preserves 161 frames
model.speaker_selection   = "coordinates"      <- never "default", never "auto-detect"
model.speaker_frame       = 60                 <- inside MAC's line; 2.279-3.959 s at 16 fps = frames ~36-63
model.speaker_x           = (pending)
model.speaker_y           = (pending)
```

Isolated in its own job. One run.

## 6. Reply with

1. The three PNG storage keys from the frame extract at `batch_index` 20 / 60 / 120.
2. Confirmation the new tab is named `fx-dub v3-sync` and that nothing else was touched.
3. Whether `LoadVideo` resolved the asset key with no upload — the first half of the VIDEO-by-key
   question, answerable from the extract run alone.
4. Anything in section 1 you still read differently. If your view of the node genuinely shows four
   inputs after this, that is a discrepancy between our two tool surfaces and it is worth more to
   us than the audition — say so and we will chase it before spending anything.
