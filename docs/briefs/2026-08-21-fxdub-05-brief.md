PASTE TARGET: the Comfy Cloud in-app agent (the fx-dub thread). Do NOT paste into any Claude/Grok session.

---

# fx-dub round 5 — v2.1 verified clean; two drift fixes + the audition-input question (STILL VALIDATE ONLY)

We pulled "fx-dub v2.1" (33 nodes) and verified it wire-by-wire against the fix list. **F1–F6 all genuinely pass** — this is the first build that survives our detectors with zero findings. Specifically confirmed from the JSON, not your report:

- **F1** — KSampler `negative` ← `ConditioningZeroOut` ← encoder; `positive` ← encoder directly. CFG-6 guidance is real now.
- **F2** — `UNETLoader acestep_v1.5_xl_base_bf16` + `VAELoader ace_1.5_vae` + `DualCLIPLoader qwen_0.6b/qwen_4b type=ace` → AuraFlow shift 3 → euler/simple/50/cfg 6. Correct split stack.
- **F3** — `EmptyAceStep1.5LatentAudio` (seconds ← duration knob) ✔; loader `skip_first_frames=0`, `frame_load_cap=0` ✔; `VHS_SelectEveryNthImage(30)` sits on the Florence branch **only**, and the mux reads the loader's full IMAGE stream ✔. That was the subtle one and you got it right.
- **F4** `timesignature "4"` ✔ · **F6** mux h264-mp4, `frame_rate ← source_fps`, audio ← mix ✔.
- **F5 answer accepted** — no float-math node on the allowlist; the manual duration knob stands, documented as a knob. Your structural FLOAT→FLOAT browse is exactly the right way to answer that.

Your v2.1 graph is now archived in our repo and is the first graph to pass an automated defect suite we built this session (7 detectors, proven to fire red on the older graphs).

## Two corrections to carry

1. **Unrequested drift: the Florence task changed.** v2 used `more_detailed_caption`; v2.1 has `detailed_caption`, while your report said the Florence block was "carried over". Please set it back to **`more_detailed_caption`** (a richer caption is the whole input to the audio-prompt stage). Not a big defect — but the class of thing matters: when you report "carried over unchanged," widget values need to match too, not just topology.
2. **`VHS_VideoCombine` does expose those knobs.** You reported it "has no trim_to_audio, yuv420p, crf, or save_metadata widgets in its schema." The pulled graph JSON shows all four present and already set correctly (`pix_fmt yuv420p`, `crf 19`, `save_metadata true`, `trim_to_audio false`). No action needed — the values are right; just noting your schema view under-reported the node so you don't design around a missing knob later.

## The audition input — a real question we need you to answer

The Director's audition should run on **your 10-second rain/cyborg clip**, not `bedroom.mp4`. We verified that clip locally: **161 frames, 10.062 s, ~16 fps, h264, single video track, no audio** — an ideal silent test subject, and its length matches the 10 s duration knob exactly.

Problem: it was written to **outputs**, and `VHS_LoadVideo` reads from **inputs**. So:

- **Q1.** Can you get that clip into the loader's input list (a node/route that writes to inputs, a save-to-input path, or re-generating it directly into inputs)? If there is no path from outputs → inputs, say so plainly and we will upload it from our side.
- **Q2 — a real risk we found in that file.** Its MP4 **movie header (`mvhd`) carries timescale 0** (malformed); only the per-track `mdhd` is sane (16384 / 164864 = 10.062 s). `VHS_VideoInfo.source_fps` drives `VHS_VideoCombine.frame_rate`, so if fps derivation reads the movie header it may emit 0 or NaN and produce a broken dub. **Before the audition, check what `source_fps` actually reports for this clip** (a preview/inspect is fine — no generation). If it isn't ~16, we wire the mux frame_rate to a primitive instead.

Also: with `select_every_nth=30` on a 161-frame clip, Florence sees ~6 frames. That's sensible — no change requested; just confirming the arithmetic is intentional.

## Cost note (not a criticism — a planning fact)

The two video-generation jobs cost **173 + 512 gpu-sec (~180 credits total)**. For scale, an entire fx-dub audio run is ~10–15 credits. We are keeping that clip as **the** standing audition fixture rather than generating new footage — please don't regenerate test video unless the Director asks.

## Reply with

1. Q1 answer (path from outputs → inputs, or "no path").
2. Q2 answer (what `source_fps` reports for that clip).
3. Confirmation the Florence task is back to `more_detailed_caption` (and the tab's validation state after the change).

**Still no runs.** Once Q1/Q2 come back, the Director orders the audition: one pinned-seed run on that clip. Our side then decodes the mix FLAC header (this finally settles the mix-bus sample-rate question your Q2 flagged two rounds ago), reads both LUFS manifests, and confirms the dubbed MP4 plays with audio.
