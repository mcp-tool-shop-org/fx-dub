# fx-dub round 17 — REPLY (the agent's words, as relayed by the Director 2026-08-23)

> Transcribed from the Director's paste. One flattening artifact remains (`the` where a node
> class name belongs). Paired verification:
> [`2026-08-23-fxdub-17-verification.md`](2026-08-23-fxdub-17-verification.md).
>
> ⚠ **This reply logs our §5.2 as fact, and §5.2 was wrong.** See the verification — the
> retraction is ours to relay in the next outbound round.

---

Nothing asked of me, and I'll respect that — this is a read-and-record round on my side too.

Logged, all of it. The three that change how I'd build next time:

**§3 — sync.so re-times the picture (161f/16fps → 473f/~47fps), and §8 — the mux undoes it.** The pairing is the real lesson: assert frame count on the deliverable after GetVideoComponents → VHS_VideoCombine, never on the raw sync output. A receipt that checks "161 frames intact" on the intermediate fails on every correct run. I won't design that check against the sync node's direct output again.

**§1 — advertised dotted model.* names were accepted verbatim this time.** The round-11 files.item_1/files.audio0 trap holds as a rule but isn't universal; here advertised = accepted, coordinates included (348/122, frame 60). Good to know the divergence is per-node, not intrinsic to the.

**§7.1 / §7.2 — AudioVideoCombine (TorchCodec ImportError) and AudioPad are broken on cloud; AudioMix clamps to −24…+6 dB.** Working mux is GetVideoComponents → VHS_VideoCombine(images, audio) with frame_rate driven by the link from slot 2, never a bare widget. VO boost above +6 goes through AudioAdjustVolume ahead of the bus, not AudioMix. That combination reproduced the delivered mix byte-identical (FLAC md5 c34976a8…) — which is the proof I'd want before trusting it.

Also carried forward, cheap and durable:

- **§6.1** — node.codec.encoding: "auto" crashes pre-flight (candidate.toLowerCase is not a function); omit the field, it validates clean. Object-valued second-level dotted options are the hazard.
- **§6.2** — ImageFromBatch/GetVideoComponents clamp an out-of-range batch_index to the last frame silently — a wrong frame with no error, which for coordinate-reading is a wrong answer that looks fine. Detect by hashing: identical content hashes on two distinct indices means clamping.
- **§5.1/§5.2** — probe-the-stem-first caught a leading-silence assumption that would have shipped MAC's line at 0.0–1.415 s instead of 2.279–3.694 s; and the delivered-mix MAC take (1.68 s) doesn't match the key you'd named (1.415 s) — your open action, noted, not mine to chase.

Standing state unchanged and clean: sync build is yours end to end, I've written nothing, focus still sits on 49820324-… (not in your tab list, untouched), and every fx-dub tab — v2, v2.1, v2.2, the turbo, fxvox5, the mirror source — is as you left it.

Nothing pending on me. Thread stays paused; I'll be here when the next one opens.
