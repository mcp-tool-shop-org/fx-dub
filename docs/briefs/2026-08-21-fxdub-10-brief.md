PASTE TARGET: the Comfy Cloud in-app agent (the fx-dub thread). Do NOT paste into any Claude/Grok session.

---

# fx-dub round 10 — your asset ID was right (we were wrong), and here is the real turbo recipe

## 1. Correcting ourselves, on the record

You identified the clip as `video/ComfyUI_00001_.mp4` / `ea68c5aa…mp4`. We queried
`LoadVideo.file`'s COMBO from our side, did not see that hash among its nine entries, and
concluded the value was invalid and the audition would fail validation.

**That was our error, not yours.** We were reading the input/attachment namespace; the clip is
a cloud `SaveVideo` output living in the asset store under a storage key that `LoadVideo`
resolves but that COMBO does not enumerate. Your evidence — the platform's own validator
returning `ready_to_run: true, error_count: 0` — was the better probe, and we should have
weighed it that way. Absence from a COMBO listing is not proof of invalidity. It is in our
trap ledger now so it does not happen again.

## 2. Your declared gap is closed — you do not need another read path

You flagged, correctly and usefully, that the per-stream detail (161 frames / `avg_frame_rate`
16/1 / avc1) was truncated before it reached you, so your ID rested on duration + single
stream + elimination + provenance rather than the full five-point signature.

We closed it from our side. `get_output` on the source job
(`1c4e02a8-0a7f-4806-b548-201160f42530`) returned `filename:
ea68c5aada3b35b0c8be343f52671cf40ef30066b7d2852766ffe86f1292e5c9.mp4`, disposition
`video/ComfyUI_00001_.mp4`. We downloaded those exact bytes and probed them locally:

- **161 frames**
- **10.0625 s**, track timescale **16384** (= `time_base 1/16384`)
- **one video track, no audio track**
- 963,326 bytes, sha256 `9985a8ba6197ea7c02adc99c4c3aafc2a9d1cfa13e535ede64908ebea327a30c` —
  **byte-identical to our local fixture**
- 161 / 10.0625 = **16.000 fps exactly**, so the `PrimitiveFloat` 16.0 pin is confirmed correct

Your identification was right on all five points, including the two you declined to claim.
**`fx-dub v2.2` is cleared on the loader.** Thank you for flagging the gap instead of papering
over it — that is what made this checkable.

Also verified independently from the pulled JSON: `LoadVideo` → `GetVideoComponents`, IMAGE to
*both* consumers, `VHS_VideoInfo` and `VHS_LoadVideo` gone, fps/AUDIO outputs left unconnected,
Florence on `more_detailed_caption`, all seven prefixes under `fxdub22/`, and the
`PrimitiveFloat` 16.0 → `VHS_VideoCombine.frame_rate` link present on input slot 4. Clean.

## 3. The turbo tab — round 8 reached you truncated, AND its instruction was wrong

You reported round 8 cut off at *"Set the sampler block to whatever the official ACE-Step 1.5
turbo template…"*, so you left the XL values in place and labelled them a placeholder rather
than inventing a recipe. **That was exactly the right call** — a guessed recipe would have
produced a meaningless A/B that still looked valid.

Two things you could not have known:

**(a) The relay was truncated on our side.** Not your failure.

**(b) Round 8's own instruction was wrong.** It told you to replace the three split loaders
with `CheckpointLoaderSimple` → `ace_step_1.5_turbo_aio.safetensors`. We have now read the
official template — `audio_ace_step1_5_xl_turbo` (13 nodes,
`https://cloud.comfy.org/templates/audio_ace_step1_5_xl_turbo.json`) — and it does **not** use
a checkpoint loader. It keeps the same split stack v2.2 already has. Our expectation was the
thing that was wrong, which is exactly why that brief told you to trust the template over it.

### What the official turbo template actually specifies (quoted from the template JSON)

| Node | Widget | Template value |
|---|---|---|
| `UNETLoader` | `unet_name` | **`acestep_v1.5_xl_turbo_bf16.safetensors`** |
| `UNETLoader` | `weight_dtype` | `default` |
| `VAELoader` | `vae_name` | `ace_1.5_vae.safetensors` |
| `DualCLIPLoader` | `clip_name1` / `clip_name2` | `qwen_0.6b_ace15.safetensors` / `qwen_4b_ace15.safetensors` |
| `DualCLIPLoader` | `type` / `device` | `ace` / `default` |
| `ModelSamplingAuraFlow` | `shift` | **`3`** (unchanged) |
| `KSampler` | `steps` | **`8`** |
| `KSampler` | `cfg` | **`1`** |
| `KSampler` | `sampler_name` | `euler` (unchanged) |
| `KSampler` | `scheduler` | `simple` (unchanged) |
| `KSampler` | `denoise` | `1` (unchanged) |
| `TextEncodeAceStepAudio1.5` | `cfg_scale` | `2` (unchanged) |

Template `KSampler.widgets_values` verbatim: `[0,"fixed",8,1,"euler","simple",1]`
(seed, control_after_generate, steps, cfg, sampler_name, scheduler, denoise).

### So the A/B is far tighter than round 8 described. Rebuild the generator block as:

**Restore the split stack** — `UNETLoader` + `VAELoader` + `DualCLIPLoader`, exactly as in
v2.2 — and remove the `CheckpointLoaderSimple`. Then change **only these three values** from v2.2:

1. `UNETLoader.unet_name` → `acestep_v1.5_xl_turbo_bf16.safetensors`
2. `KSampler.steps` → `8`
3. `KSampler.cfg` → `1`

Everything else stays byte-for-byte v2.2: same VAE, same dual CLIP, AuraFlow shift 3, euler /
simple / denoise 1, encoder `cfg_scale` 2, the shared seed, the tags and dialogue primitives,
duration 10, the Florence block, the mix bus, the meters, the three FLAC savers, the mux.

If `acestep_v1.5_xl_turbo_bf16.safetensors` is not present in `UNETLoader`'s list, **stop and
tell us** — do not substitute the AIO checkpoint to make it work. A three-value delta is a
controlled experiment; a loader-topology swap is not, and we would rather have no A/B than an
uninterpretable one.

### Naming

You built the tab as `fx-dub v2.1-turbo`, but you copied it from v2.2 (correctly — it carries
the `LoadVideo` → `GetVideoComponents` ingest). The name now misstates its lineage. **Rename it
`fx-dub v2.2-turbo`.** Graph names are how we track provenance across sessions.

## 4. Two cleanups in `fx-dub v2.2` (neither blocks the audition)

1. **`Florence2Run.control_after_generate` is `randomize`.** Every other seeded node in the
   graph is `fixed` (`KSampler`, `TextEncodeAceStepAudio1.5`, the `PrimitiveInt`). The value
   submitted for this run is `seed: 1`, so results are unaffected — but the canvas seed mutates
   after each run and the tab stops being byte-for-byte replayable. **Set it to `fixed`.**
   Apply to the turbo tab as well.

2. **`FL_ChatterboxTTS.seed` is `0` and is not wired to the shared `PrimitiveInt(1)`.** The
   node spec gives `seed` as `INT, default 0, min 0`, so `0` is a legal literal and the VO may
   well already be deterministic — we have not measured it. Please answer one question rather
   than changing anything: **does `FL_ChatterboxTTS` treat `seed: 0` as a literal seed, or as a
   "randomize" sentinel?** If you cannot determine it from the node's implementation, say so
   and we will settle it with a repeat run after the audition. Do not guess.

## 5. Reply with

1. `fx-dub v2.2-turbo`: validation state (errors / warnings / ready_to_run / node count), and
   confirmation that the generator block is the split stack with exactly the three changed values.
2. Whether `acestep_v1.5_xl_turbo_bf16.safetensors` was actually available in `UNETLoader`.
3. Confirmation that `Florence2Run.control_after_generate` is now `fixed` in **both** tabs.
4. Your answer on the `FL_ChatterboxTTS` seed-0 semantics (or an explicit "cannot determine").
5. Confirmation that `fx-dub v2.1` and `fx-dub v2.2` are otherwise untouched.

**Still no runs.** v2.2 is cleared on the loader and the Director orders the audition next;
the turbo tab auditions alongside it on the same clip so the credit and quality difference is
measured, not estimated.
