# Step-4 verification record — fx-dub v1 dispatch

**Date:** 2026-08-21 · **Runner:** `roleos verify-citations` (role-os 2.10.0 local clone) → `prism verify --type citations --provider ollama` (prism 1.6.0; groundedness lens `mistral-small:24b`, ModelFamily `local`, reasoning-stripped — family-different from the `anthropic` synthesizer by construction; deterministic arXiv/Crossref retrieval oracle for existence).

## Result

- **Verdict:** `escalate` (advisory tier — the gate discriminates, it never rubber-stamps). Exit 30.
- **20 citations checked, 54.7 s. Existence: 20/20 resolved. 0 fabricated. 0 misattributed.**
- **Groundedness:** 7 supported at title+abstract; **13 `RETRIEVE FULL TEXT`** — the claimed numbers live in paper bodies (tables/ablations), not abstracts. All 13 were resolved **out-of-band at research time**: the Step-2 agents fetched the full HTML texts and quoted the exact passages (per-lane `tool_uses` receipts 18/28/24/38/29, quotes archived in the dispatch's source notifications). This is the same escalation class documented in the protocol's 2026-06-29 receipt.
- **6 items unparsed** (no arXiv/DOI id): the EBU/Netflix/Spotify/ATSC/Google spec pages and multi-source rows — the oracle is arXiv/Crossref-tuned by design (known limitation, v1.2 lesson); each carries a direct quote fetched by the research agent from the primary page.
- **First run halted correctly:** prism refused to run without a signing key (`verifier_unreachable`, 0 checked) and the runner escalated rather than reading absence as "citations fine" — the andon behaving as specified. Fixed via `prism keygen`; re-run signed.

## Receipt chain

- roleos receipt (committed): [`2026-08-21-fxdub-v1.dispatch.citation-receipt.json`](2026-08-21-fxdub-v1.dispatch.citation-receipt.json) — `chain_sha256 4c2fb42069464b35…`, `citations_sha256 cf7668438e179e4a…`, per-citation `retrieval_pins`.
- prism canonical receipt (committed): [`2026-08-21-fxdub-v1.dispatch.prism-canonical.json`](2026-08-21-fxdub-v1.dispatch.prism-canonical.json) — id **`prism-01m0k6mbv7sh918mhja9bxpszt`**, alg **Ed25519**, `prism verify-receipt --public-key` → **`signature_valid: true`, exit 0** (verified with [`2026-08-21-prism-pub.pem`](2026-08-21-prism-pub.pem) alone — no shared secret).
- Honest ceiling (standing prism disclosure): the signing key is ephemeral and session-local, so the receipt buys third-party *verifiability*, not anti-forgery.
- Note: the summary block embedded in the roleos receipt re-serializes the display object and does NOT signature-verify — verify the canonical `prism replay` export only (documented lesson, 2026-06-29).

## Disposition of advisory items

| Class | Count | Disposition |
|---|---|---|
| Supported (abstract-level) | 7 | Load-bearing as cited. |
| Retrieve-full-text | 13 | Load-bearing; full text was retrieved and quoted by the research agents on 2026-08-21. |
| Unparsed (spec/vendor URLs) | 6 | Load-bearing; primary pages fetched and quoted by the research agents on 2026-08-21. Not oracle-checkable (non-arXiv/DOI) — cite primary URLs per the v1.2 lesson. |
| Provenance-caveat | 1 (finding 18, Movie Gen ablation numbers) | **NOT load-bearing** — marked advisory in the dispatch itself; the agent disclosed its numbers came from a search index, not a direct fetch. Architecture choice D does not depend on it. |
